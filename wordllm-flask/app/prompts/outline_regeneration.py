"""
章节大纲重新生成的提示词模板
"""

# 重新生成大纲的提示词模板
OUTLINE_REGENERATION_PROMPT = """
你是一位经验丰富的文档结构专家，专门负责优化文档章节大纲。请根据以下信息重新生成一个更优质的章节大纲：

===模板内容===
{template_content}
===模板内容结束===

{input_content_section}

{special_instructions}

请注意：
- “模板内容”决定文档的章节结构和章节名称，必须严格按照模板内容的结构和风格生成章节目录。
- “输入文件内容”仅作为参考资料或上层依据文件，可以辅助理解背景，但不得直接用于生成章节目录，不得抄袭输入文件的章节结构。

请生成一份高质量的章节大纲，满足以下要求：

1. 请确保大纲主要包含第1级和第2级章节
2. 每个章节的编号格式为数字，例如"1"、"1.1"，其中：
   - 一级章节使用单一数字："1"、"2"、"3"...
   - 二级章节使用两级数字，如："1.1"、"1.2"、"2.1"...
3. 每个章节都必须有一个恰当且描述性强的标题
4. 章节内容应该全面覆盖模板中的主题和领域
5. 章节结构应该逻辑清晰、层级合理、组织有序
6. 请以JSON格式返回结果，格式如下：

```json
{{
  "chapters": [
    {{
      "chapterNumber": "1",
      "title": "章节标题1"
    }},
    {{
      "chapterNumber": "1.1",
      "title": "章节标题1.1"
    }},
    {{
      "chapterNumber": "1.2",
      "title": "章节标题1.2"
    }}
  ]
}}
```

请确保JSON格式正确，没有语法错误或缺失字段。只返回JSON，不要添加其他说明或注释。
"""

# 有输入文件时的内容部分
INPUT_CONTENT_SECTION = """
===输入文件内容（仅作参考依据）===
{input_content}
===输入文件内容结束===

注意：输入文件仅作为参考资料或上层依据文件，不应直接决定章节结构。章节结构应主要基于模板内容来设计。
"""

# 无输入文件时的内容部分
NO_INPUT_CONTENT_SECTION = """
没有提供输入文件内容，请仅基于模板内容构建大纲。
"""
